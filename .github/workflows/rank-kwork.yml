name: Rank Kwork Projects

on:
  push:
    paths:
      - "rank_trigger.txt"

permissions:
  contents: write

jobs:
  rank:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Rank projects
        run: python rank_projects.py
      - name: Build market summary
        run: python market_summary.py
      - name: Build compact market summary
        run: python market_compact.py
      - name: Commit ranking and market summaries
        shell: bash
        run: |
          git config user.name "kworkworks-bot"
          git config user.email "kworkworks-bot@users.noreply.github.com"
          git add data/ranking.json data/market_summary.json data/market_summary_compact.json
          if git diff --cached --quiet; then
            echo "No ranking changes"
            exit 0
          fi
          git commit -m "Update Kwork ranking and market summaries"
          git push
